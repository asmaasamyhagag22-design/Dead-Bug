# PROCESS — Dead Bug AQA

> وثيقة كاملة: المشروع ده بيحل إيه، ليه اتبنى بالشكل ده، وكل مشكلة تقنية قابلتنا واتحلت إزاي.
> الكود والمصطلحات التقنية بالإنجليزي، الشرح بالعربي.

---

## الجزء ١ — ليه المشروع أصلاً؟

### المشكلة

تمرين **Dead Bug** من أهم تمارين تثبيت الجذع (core stability) في إعادة التأهيل. الشخص بينام على ضهره، الوركين والركبتين ٩٠°، وبيمدّ ذراع مع الرجل المقابلة (contralateral).

**الغلطة الأساسية في التمرين هي إن أسفل الظهر يتقوّس ويرتفع عن الأرض** (lumbar arch). لو ده حصل، التمرين بيفقد قيمته كلها وممكن يبقى ضار.

### ليه ده مش مسألة سهلة على الكمبيوتر

هنا المفارقة اللي بنى عليها المشروع كله:

> **الغلطة الأساسية في التمرين غير مرئية تماماً في الـ skeleton.**

MediaPipe بيدي ٣٣ landmark. **ولا واحدة منهم على العمود الفقري.** فيه كتفين وفيه وركين، وما بينهم لا شيء. يعني لو الظهر اتقوّس، الـ keypoints ممكن تفضل زي ما هي بالظبط.

أي نظام بيعتمد على الـ keypoints بس **مش هيشوف الغلطة الأساسية في التمرين**. دي فجوة حقيقية، ومنها جت المساهمة.

### الحل المقترح — إشارة القطن من الـ silhouette

بدل ما نعتمد على نقاط الهيكل، بنستخدم **الـ segmentation mask** (ظل الجسم) اللي MediaPipe بيطلعه كمان:

```
u              = اتجاه الوحدة من مركز الحوض ناحية الكتف
النطاق القطني  = x ∈ [0.00, 0.35] × torso_len على اتجاه u
bottom(x)      = أوطى بكسل في الـ silhouette عند العمود x
gap(x)         = max(0, floor_y(x) − bottom(x))
lumbar_gap(t)  = Σ gap(x) ÷ torso_len_px²
```

يعني: **المساحة المحصورة بين الظهر وخط الأرض**، مقسومة على مربع طول الجذع.

القسمة على `torso_len²` هي اللي بتخلي الإشارة **بلا وحدة ومستقلة عن بُعد الكاميرا وحجم الجسم** — مساحة ÷ طول². ده معناه إن نفس القوس على شخص قريب من الكاميرا وشخص بعيد بيدّي نفس الرقم.

---

## الجزء ٢ — الواقع اللي غيّر التصميم

### الداتا اللي موجودة فعلاً

| | |
|---|---|
| عدد الملفات | ١٠ (بينهم نسخة مكررة بالظبط بالـ md5) |
| الـ label | `correct` **فقط** |
| المصدر | YouTube + تسجيلات شخصية |
| الـ fps | مختلط: 23.98 / 25 / 29.97 / 30 |
| الأبعاد | مختلطة: 640×360، 1006×540، 500×382، 1280×720 |

### القيد اللي بيمنع الحل التقليدي

**مستحيل تدريب multi-class classifier على الداتا دي.** كل الكليبات `correct`. لو حاولنا، هيحصل واحد من اتنين:

1. نولّد الأخطاء صناعياً ونقيّم عليها → **دائري تماماً**، الرقم بلا معنى
2. نستخدم `lumbar_gap` نفسها تعمل labels والموديل ياخدها input → **دائري برضه**

النقطة التانية دي أخطر واحدة في المشروع كله، ومكتوبة في الكود نفسه:

> **ممنوع تماماً إن `lumbar_gap` تولّد الـ labels *و* تبقى input للموديل في نفس الوقت.**
> ده circular labeling — الموديل هيتعلم يعيد إنتاج العتبة بتاعتك، الـ macro-F1 هيطلع عالي، والرقم مالوش أي معنى.

### التمييز اللي حل المشكلة كلها

```
صلاحية الأداة (Instrument validity)  ≠  تقييم الكاشف (Detector evaluation)
```

| | السؤال | محتاج داتا أخطاء؟ |
|---|---|---|
| **صلاحية الأداة** | هل `lumbar_gap` بيقيس ارتفاع الظهر فعلاً؟ | ❌ يتثبت بالفيزياء والتشويش الصناعي |
| **تقييم الكاشف** | هل بيمسك الأخطاء الحقيقية؟ | ✅ **مفيش بديل** |

الجزء الأول اتعمل بالكامل من داتا سليمة بس. الجزء التاني محتاج ~٤٠ rep غلط.

---

## الجزء ٣ — المعمار: مسارين بكود تقييم واحد

```
┌──────────────── MASAR A ─────────────────┐
│  Rehab-Pile / KIMORE / KERAAL            │
│  (منشورة · مصنّفة · فيها أخطاء حقيقية)    │
│                                          │
│  الغرض: إثبات إن كود التدريب والتقييم     │
│         سليم + جدول أرقام قابل للدفاع     │
└────────────────┬─────────────────────────┘
                 │  modeling/evaluate.py
                 │  (نفس macro_f1 · نفس LOSO ·
                 │   نفس leakage assertion)
┌────────────────▼─────────────────────────┐
│  MASAR B — Dead Bug                      │
│  (مفيش dataset عام · correct فقط)         │
│                                          │
│  الغرض: المساهمة الأصلية                  │
│  المخرج: إنذار كاذب + صلاحية الأداة        │
└──────────────────────────────────────────┘
```

**الربط مش شكلي.** `modeling/evaluate.py` مكتوب **numpy-only عن قصد** — لا sklearn ولا aeon — عشان المسارين يقدروا يستوردوه من بيئتين مختلفتين. ده الكود المشترك فعلياً: نفس `macro_f1`، نفس `loso_folds`، نفس `wilson_interval`، نفس `assert_no_group_leak`.

---

## الجزء ٤ — المشاكل التقنية وحلولها

دي أهم حتة في الوثيقة. كل مشكلة هنا **اتكشفت بتشغيل الكود على الداتا الحقيقية**، مش بقراءة الوثائق.

---

### ٤.١ ⛔ MediaPipe بيقتل الـ process بدون أي traceback

**العرض:** تشغيل الـ pipeline على `Recording 2026-07-25 171125.mp4` بيموّت الـ Python interpreter فوراً:

```
F0000 image_frame.cc:415] Check failed: 1 == ChannelSize() (1 vs. 4)
*** Check failure stack trace: ***
```

مفيش Python exception، مفيش traceback، مفيش `try/except` ينفع يمسكها. الـ process بيموت خالص.

**التشخيص:** بالعزل خطوة بخطوة:
- الـ pose detection شغال ١٠٠٪ على نفس الكليب (101/101 frame)
- الانهيار بيحصل بالظبط عند `segmentation_masks[0].numpy_view()`
- كل الكليبات التانية شغالة

**السبب الحقيقي:** محاذاة الصفوف في الذاكرة (row alignment).

| العرض | العرض × 4 بايت | ÷ 16 | النتيجة |
|---|---|---|---|
| 640 | 2560 | 160 ✓ | شغال |
| 1280 | 5120 | 320 ✓ | شغال |
| 500 | 2000 | 125 ✓ | شغال |
| **1006** | **4024** | **251.5 ✗** | **انهيار** |

**الحل:** تقريب أبعاد الفريم لأقرب مضاعف لـ 16 قبل الـ inference:

```python
# src/deadbug/ingest/video_source.py
def snap(value: int, to: int = 16) -> int:
    return max(to, int(round(value / to)) * to)
```

`1006×540 → 1008×544`. اتأكدنا: الانهيار اختفى، detection ١٠٠٪، الـ mask بيتقرا عادي.

**ليه ده مهم:** ده نوع الـ bug اللي ممكن ياكل يوم كامل، لأن مفيش أي رسالة خطأ في Python تدلّك على السبب.

---

### ٤.٢ MediaPipe VIDEO mode — detector واحد لكل كليب

**العرض:** انهيار مشابه لما بنعمل loop على كذا كليب بـ detector واحد.

**السبب:** `RunningMode.VIDEO` بيحتفظ بحالة tracking بين الاستدعاءات، وبيطلب timestamps **تصاعدية بشكل صارم**. لما بنعدّي على كليب جديد:
- الـ timestamps بترجع تبدأ من صفر
- أبعاد الفريم بتتغير

**الحل:** إنشاء backbone جديد لكل كليب. `scripts/run_pose.py` الأصلي كان عامل كده صح — وده سبب إننا **refactor**نا الملف بدل ما نعيد كتابته من الـ spec.

```python
timestamp_ms = int(frame_index / fps * 1000)   # من رقم الفريم، مش من ساعة الحائط
```

---

### ٤.٣ ⚠️ `view_score` لازم يتحسب في الـ pixel space

**الخلفية:** `view_score = hip_sep / torso_len` — مقياس مجاني لزاوية الكاميرا. في الـ side view المثالي محور الوركين موازي لاتجاه النظر، فبيتسقط لصفر تقريباً.

**المشكلة:** MediaPipe بيرجع إحداثيات normalized في `[0,1]`. لكن **x و y ليهم مقاييس بكسل مختلفة!** في فريم 1280×720، الوحدة في x = 1280 بكسل والوحدة في y = 720 بكسل.

في الوضع المستلقي ده أسوأ حالة ممكنة: **الجذع ماشي على محور x والوركين بيتباعدوا على محور y** — يعني البسط والمقام كل واحد متأثر بمقياس مختلف.

**القياس على كليب حقيقي:**

| | القيمة | الحكم |
|---|---|---|
| normalized (غلط) | 0.240 | `REJECT` |
| pixel (صح) | **0.131** | `oblique` |

الفرق بيقلب الحكم على الكليب.

**الحل:** `view_score(kpts, frame_size=(w, h))` بتضرب في الأبعاد الأول. والـ config فيه `triage.view_space: pixel` صراحةً.

---

### ٤.٤ 🐛 الـ floor inlier ratio كان بيقيس حاجة تانية خالص

دي أخطر bug صامت اتكشف، لأنه كان هيدمّر الـ QC كله بدون ما يبان.

**العرض:** على أنضف كليب عندنا (`videoplayback (3)`)، الـ `floor_inlier_ratio` طلع **0.66** — أقل من عتبة الرفض 0.80. يعني الكليب مرفوض.

**بس:** أوطى صف في الـ silhouette كان **138 بالظبط في كل الـ 899 فريم** (المدى 138–140). يعني الأرضية ثابتة تماماً والتقدير سليم ١٠٠٪.

**السبب:** الـ metric نفسه كان غلط. كنت بحسب "نسبة الأعمدة اللي bottom(x) بتاعها على خط الأرض" — بس **في الـ Dead Bug الأطراف مرفوعة في الهوا عمداً!** فأغلب الأعمدة عمرها ما هتلمس الأرض.

يعني:

> الـ metric كان **عكسياً مع الأداء الصحيح**. كل ما الشخص يعمل التمرين صح (أطراف مرفوعة)، كل ما الـ inlier ratio ينزل، وكل ما احتمال رفض الكليب يزيد.
>
> البوابة اللي المفروض تحمي جودة الداتا كانت هترمي بالظبط الكليبات اللي هي موجودة عشانها.

**الحل:** الـ fit والـ inlier ratio الاتنين بيتحسبوا على **أوطى شريحة من الـ silhouette بس** (أكبر قيم y)، مش على كل الأعمدة:

```python
cutoff = np.percentile(y, 100.0 - calib_fallback_percentile)
keep = y >= cutoff
```

كده `inlier_ratio` معناه "هل النقاط اللي فعلاً بتلمس الأرض واقعة على خط مستقيم؟" — وده اللي بيكشف كاميرا مايلة أو بتتحرك، وهو الغرض الأصلي.

**النتيجة بعد الإصلاح:** الطريقتين (percentile و RANSAC) الاتنين بيدّوا `inlier_ratio = 1.000` ومتفقين على موقع الأرض بشكل مستقل (139 مقابل 138 بكسل).

---

### ٤.٥ تعارض في الـ dependencies + فخ الـ `aeon==0.0.0`

**المشكلة الأولى:** `aeon` بيطلب سقوف أقل من اللي متثبت:

| الحزمة | متثبت | aeon بيطلب |
|---|---|---|
| numpy | 2.5.1 | `<2.5.0` |
| pandas | 3.0.5 | `<2.4.0` |
| scipy | 1.18.0 | `<1.18.0` |

تثبيت aeon في نفس البيئة كان هينزّل التلاتة تحت MediaPipe و OpenCV الشغالين.

**الحل:** بيئة منفصلة `venv-a` للمسار A. البيئة الأصلية مالهاش دعوة.

**المشكلة التانية — وده فخ حقيقي:** لما حطينا `numpy<2.5` كقيد صريح، uv اختار numpy 2.4.6. لكن `numba` (اللي aeon محتاجه) بيطلب `numpy<2.4`. بدل ما uv يرجع يقلل numpy، **راح مثبّت `aeon==0.0.0`** — وده package فاضي placeholder على PyPI!

**النتيجة:** التثبيت خلص بـ **exit code 0** والـ log مليان `+ aeon==0.0.0`، وكل حاجة شكلها ناجح. بس `import aeon` بيفشل.

> resolve مستحيل بيبان زي نجاح.

**الحل:** تثبيت `aeon==1.5.0` صراحةً وسيب الـ resolver يرجّع numpy لـ 2.3.5 لوحده.

**البيئة النهائية الشغالة:**
```
aeon 1.5.0 · tensorflow 2.21.0 · keras 3.15.0
numpy 2.3.5 · scipy 1.17.1 · pandas 2.3.3 · scikit-learn 1.8.0
```

---

### ٤.٦ قائمة أسماء الـ datasets بتتسحب من النت

`load_rehab_pile_classification_datasets()` بتعمل HTTP scraping لموقع خارجي. لو فشلت، بترجع **list فاضية**، وبعدين أول محاولة تحميل بترمي `Dataset not found` — فتفضل تدوّر على غلطة إملائية في اسم صحيح.

**الحل:** `REHABPILE_FOLDS` هو dict **مكتوب بالكود** في نفس الملف، فالبديل مضبوط مش تخمين:

```python
# src/deadbug/modeling/rehabpile.py
def ensure_registry() -> None:
    import aeon.datasets.rehabpile_loader as R
    try:
        found = R.load_rehab_pile_classification_datasets()
    except Exception:
        found = []
    if not found:
        R._rehabpile_classification_datasets = sorted(R.REHABPILE_FOLDS["classification"])
        R._rehabpile_regression_datasets = sorted(R.REHABPILE_FOLDS["regression"])
```

الأسماء المتحقق منها: `KIMORE_clf_bn_{LA,LT,PR,Sq,TR}` = 5 folds لكل واحد · `KERAAL_clf_mc_{CTK,ELK,RTK}` = 6 folds.

---

### ٤.٧ LITEMV لازم يتستورد جوه الدالة

`LITETimeClassifier` بيبني كائنات Keras في `__init__` نفسه — يعني بيرمي exception عند **الإنشاء** لو TensorFlow مش موجود، مش عند `fit`.

لو الاستيراد على مستوى الملف، الملف كله يبقى غير قابل للاستيراد من غير TF — **وياخد معاه الـ sklearn baselines** اللي مالهاش أي علاقة بـ TensorFlow.

```python
def fit_predict_litemv(...):
    from aeon.classification.deep_learning import LITETimeClassifier   # ← جوه الدالة
```

كمان: aeon بيكتب checkpoint لكل `fit` في `file_path` اللي افتراضيه `'./'` — يعني بينثر ملفات `.keras` في جذر الريبو. الـ config بيثبّته على `models/` والـ `.gitignore` فيه `*.keras`.

---

### ٤.٨ مفيش ffmpeg — واتشال من المسار الحرج أصلاً

`ffmpeg` و `ffprobe` و `yt-dlp` مش موجودين على الجهاز. الـ spec الأصلي كان بيستخدم ffmpeg لتوحيد الـ fps على 30 (إجباري، لأن المصادر مختلطة 23.98/25/29.97/30).

**الحل — أنضف من تثبيت ffmpeg:**
1. نحمل الـ fps الحقيقي لكل كليب (OpenCV بيقراه صح)
2. كل معامل زمني بالثواني، مش بالفريمات
3. نعمل resample للـ **إشارات** بعد استخراج الـ pose، مش للفيديو

```python
resample_to_fps(signal, fps_in, fps_out=30.0)
```

والقص (sub-clipping) بقى **seek** في `VideoSource` مش إعادة ترميز.

المكسب: dependency أقل، مرحلة CLI أقل، ومفيش خسارة جودة من إعادة الترميز.

**تفصيلة مهمة:** استخدمنا `floor` مش `round` في حساب عدد العينات الناتجة — لأن التقريب لأعلى بيخلي آخر عينة تتعدى نهاية الإدخال، وهناك `np.interp` بتثبّت القيمة وتكرر آخر فريم بصمت. أحسن نقصّر فريم من إننا نخترع فريم.

---

### ٤.٩ تعارض بين الوثائق: COCO-17 ولا native-33؟

الوثيقتين الأقدم متعارضتين:
- وثيقة ١: الـ backbone يطلع COCO-17 بإحداثيات **pixel** بشكل `(17,3)`
- وثيقة ٢: نسيب MediaPipe-33 بإحداثيات **normalized** بشكل `(33,4)`

**القرار: نسيب native 33 كصيغة تخزين وحساب.** وثيقة ١ بتخسر تلات حاجات المشروع محتاجها فعلاً:

| المفقود | بيتكسر إيه |
|---|---|
| `visibility` | بوابة الـ QC "متوسط visibility للمفاصل 11،12،23،24 ≥ 0.7" مبقاش ينفع يتحسب |
| `z` | `rotation.py` محتاج `\|z23 − z24\|` |
| الإحداثيات النسبية | الكليبات 1006×540 و 500×382 و 1280×720 و 640×360 — مفيش pixel space واحد أصلاً، والـ mask نفسه بنصف الدقة |

**التحويل بيعيش في `pose/skeleton.py`** — دالة نقية، بدون أي dependency، تتختبر في ٣ ميلي ثانية من غير فيديو ولا MediaPipe session. **مش جوه الـ backbone** لأن كده كانت هتبقى غير قابلة للاختبار بمعزل، وكل backbone جديد هيعيد اشتقاق الـ mapping من أول وجديد.

بتتنادى في **مكانين بس**: `build_features` ومقارنة الـ backbones. وفي **صفر أماكن** جوه `signals/` و `geometry/` و `segment/`.

---

### ٤.١٠ أخطر سطر في المشروع — الـ horizontal flip

الـ augmentation بالمرايا لازم يعمل حاجتين، مش واحدة:

```python
out[..., 0] = width - 1 - out[..., 0]      # ١. اعكس x
for a, b in FLIP_PAIRS:                     # ٢. بدّل شمال/يمين
    out[..., [a, b], :] = out[..., [b, a], :]
```

**نسيان الخطوة (٢) = الموديل بيتعلم labels متناقضة، والـ loss بينزل عادي وإنتي مش واخدة بالك.** مفيش أي عرض ظاهر.

وفيه نتيجتين محدش كاتبهم في الوثائق:

**أولاً: الـ flip لازم يبدّل تلات مجموعات قنوات مش واحدة:**

| القناة | التصرف |
|---|---|
| joints | اعكس x + بدّل الأزواج |
| bones | اعكس x + بدّل `[(0,1),(2,3),...]` |
| velocity | نفس تبديل الـ joints |
| **angles** | **تبديل بس، من غير تغيير إشارة** (الزوايا غير موجّهة) |
| lumbar | scalar، ثابت |

عشان ده يشتغل، شجرة المفاصل نفسها لازم تكون متماثلة تحت المرايا. فيه اختبار بيتأكد من ده:

```python
mirror(COCO17_PARENTS[c]) == COCO17_PARENTS[mirror(c)]   # لكل c
```

ودي تانية نسخة من نفس الـ bug، بس **طبقة أعمق حيث محدش بيبص**.

**ثانياً: `width - 1 - x` صحيح في الـ pixel space بس.** بعد ما `normalize()` تلف الجذع على محور +x، عكس x بيعمل مرايا **لمحور الجذع نفسه**. فالقاعدة: اعملي flip في الـ pixel space **قبل** الـ normalize، وبعدين طبّقي الـ normalize تاني على المصفوفة المعكوسة.

**اختيار الـ 8 زوايا مبني على ده:** كلهم متماثلين ثنائياً في ٤ أزواج مرايا، عشان `ANGLE_FLIP_PAIRS = [(0,1),(2,3),(4,5),(6,7)]` تبقى معرّفة أصلاً. لو ضفنا زاوية غير متماثلة (زاوية رقبة مثلاً)، كانت هتتكسر بصمت تحت المرايا.

---

## الجزء ٥ — قرارات تصميمية مهمة

### ٥.١ التطبيع: الترتيب مهم والمقياس median

```python
hip_c = mean(k[23], k[24]);  sho_c = mean(k[11], k[12])
torso_len_ref = median(‖sho_c − hip_c‖) على الكليب كله   # ← مش لكل فريم
k = k − hip_c                     # ١ إزاحة
k = k / torso_len_ref             # ٢ مقياس
k = rotate(k, torso_vec → +x)     # ٣ دوران، مع تخزين الزاوية
```

**المقياس median للكليب كله مش لكل فريم** — المقياس لكل فريم بيحقن نويز اهتزاز الحوض والكتف في كل إشارة بعد كده. فيه اختبار بيحقن ٣ فريمات مشوّهة ويتأكد إن الـ reference ما اتحركش.

**زاوية الدوران بترجع مش بتترمي** — تباينها عبر الكليب هو نفسه feature (ثبات).

### ٥.٢ الـ rep = امتداد جانب واحد

مش زوج شمال+يمين. عينات أكتر و labels أدق (ممكن جانب يكون صح والتاني غلط).

### ٥.٣ "بيستعجل" = تداخل سالب مش عتبة مدة

```
overlap_s = start(next rep) − end(this rep)
```

سالب ⇒ الـ rep اللي بعده بدأ قبل ما اللي قبله يخلص. **مستقل عن سرعة الشخص الطبيعية**، فأحسن من أي عتبة مدة مطلقة.

### ٥.٤ مفيش مدى حركة صح ثابت

المدى الصح **شخصي** — هو النقطة اللي عندها الظهر بتاع *الشخص ده* يبدأ يسيب الأرض. عشان كده:
- منحنى حدّ التحكم بيرسم `lumbar_gap` مقابل **الامتداد** مش مقابل **الزمن**
- **ممنوع** أي عتبة مسافة مطلقة للأرض في أي مكان في الكود

الشخص المتحكّم = خط أفقي. الشخص اللي بيفقد التحكم = بينكسر لفوق عند نقطة معينة — دي حدّه الشخصي.

### ٥.٥ SPARC مش LDLJ

لقياس التقطّع بنستخدم **spectral arc length**. السبب: مقاييس الـ jerk الزمنية بتتأثر بالمدة بشكل طاغي، وده كان هيخلي "متقطع" مرتبطة خطياً بـ "سريع" — وهما feature منفصلين المفروض.

---

## الجزء ٦ — البوابات (Gates)

> 🔄 **محدَّث ٣١ يوليو.** الأرقام والحالات هنا كانت قديمة.

| # | البوابة | الاختبار | الحالة |
|---|---|---|---|
| **٠** | الاختبارات الذهبية | translation 1e-6 · scale 5e-2 · rotation 1e-3 · flip involutive | ✅ **خضراء — ٣٤ اختبار** (من ١٤٢ إجمالي) |
| **١** | ⭐ الإشارة تفصل | ٥ صح مقابل ٥ قوس، منفصلين بالعين | ⏳ **محجوبة — محتاجة تصوير** |
| **٢** | كود التدريب سليم | ~~Rehab-Pile ضمن ٣٪ من المنشور~~ → **ترتيب معقول على ٣٩ dataset بالإعدادات الافتراضية للمكتبة** | ✅ خلصت |
| **٣** | التقسيم دقيق | خطأ العدّ ≤٢٪ | ⏳ **محجوبة** — مفيش كليب عنده عدد حقيقي معروف |
| **٤** | الموديل > القاعدة | MiniRocket > RF(summary) | ✅ 1.65 مقابل 2.17 متوسط رتبة |

⚠️ **بوابة ٢ اتعادت صياغتها.** "ضمن ٣٪ من المنشور" هو بالظبط ادعاء إعادة الإنتاج اللي
المشروع سحبه: إعدادات الورقة نفسها ماقدرناش نجيبها، فمفيش أساس نقارن عليه نسبة. البوابة
دلوقتي بتقيس إن كود التدريب والتقييم سليم على داتا مصنّفة فيها أخطاء حقيقية — وده اللي
الأرقام بتدعمه فعلاً.

**بوابة ٠ بتحجب كل حاجة.** ولا سطر في المسار B اتكتب قبل ما تخضر.

---

## الجزء ٧ — الاختبارات V1–V4 لصلاحية الأداة

دي الحتة اللي بتطلّع **نتائج حقيقية من داتا سليمة بس**.

| | الاختبار | الحالة |
|---|---|---|
| **V1** | استجابة للتشويش الصناعي — نرفع منطقة القطن في الـ mask بمقدار δ معلوم | ✅ **unit test** |
| **V2** | ثبات عبر الأشخاص | ⬜ محتاج كليبات أكتر |
| **V3** | ثبات ضد تغيّر المقياس والإزاحة | ✅ **unit test** |
| **V4** | الاتساق بين الإشارة الهندسية والـ silhouette | ⬜ |

**V1 و V3 اتعملوا كـ unit tests مش رسومات في notebook.** الوثائق بتوصفهم كأشكال بيانية، بس الاتنين ليهم قيم متوقعة بصيغة مغلقة، فمكانهم الطبيعي هنا حيث بيتشغلوا مع كل commit.

**V1 مش بس بيتأكد إن الاستجابة تصاعدية** — ده كان ممكن يتحقق كمان لو الإشارة بتقيس حاجة غلط خالص. بيتأكد إن **الميل نفسه** مطابق للتوقع الهندسي:

```
رفع n عمود بمقدار δ بيضيف n×δ للمساحة
⇒ الميل = n / torso_len²
```

والقياس طابق الحساب في حدود ٥٪. **ده اللي بيثبت إن الإشارة بتقيس ارتفاع الظهر مش artifact** — من غير أي rep غلط.

---

## الجزء ٨ — نتائج القياس على الداتا الحقيقية

### MediaPipe **مابيفشلش** في الوضع المستلقي

الوثيقة الأصلية مقيّمة ده كأعلى خطر، ومخصصة له بديل (RTMPose). **القياس بيكذّب ده:**

| كليب | detection | visibility (11,12,23,24) |
|---|---|---|
| videoplayback (1) | 97.8% | 0.997 |
| videoplayback (3) | 92.7% | 0.999 |
| videoplayback (6) | 90.1% | 0.992 |
| clip | 100% | 0.999 |
| Recording …171125 | 100% | 1.000 |

**٩٣–١٠٠٪ detection على ٧ من ١٠ كليبات**، وvisibility للمفاصل الأساسية ٠.٩٨٥–١.٠٠٠ على العشرة كلهم. ده نتيجة تُذكر بحد ذاتها لأنها بتخالف أدبيات الـ supine-infant OKS اللي الوثيقة نفسها بتستشهد بيها. RTMPose اتشال من خطة الأسبوع.

⚠️ التلاتة الباقيين (`Video Project 2*`) بين ١٧.٨٪ و ٧٧.١٪ — سببه شاشات عناوين وتمارين تانية جوه نفس الملف، مش فشل في وضع الاستلقاء: جوه مقاطع التمرين المكتشفة الـ detection ٩٦–١٠٠٪. الادعاء المطلق "٩٠–١٠٠٪" اتشال.

### فرز الكليبات (`view_score` بالـ pixel space)

> 🔄 **معاد القياس ٣١ يوليو على العشر كليبات.** المصدر `reports/triage.csv`.
> الجدول القديم كان كل صفوفه قديمة، وكان بيصنّف `videoplayback (6)` و
> `Video Project 2mk` **غلط كـ side view**.

| كليب | det% | view_px | view | torsoCV | الحكم |
|---|---|---|---|---|---|
| **videoplayback (3)** | 93.0 | **0.117** | **side** | 0.024 | ✅ **الجانبي الوحيد** |
| videoplayback (1) | 99.7 | 0.128 | oblique45 | **0.286** | مائل + كاميرا بتتحرك |
| clip ≡ videoplayback (4) | 100 | 0.155 | oblique45 | 0.031 | مائل (نسخة مكررة) |
| videoplayback (6) | 95.2 | **0.232** | other | **0.575** | الكاميرا بتزوّم — **مش جانبي** |
| Recording …171125 | 100 | 0.138 | oblique45 | 0.060 | مائل |
| Video Project 2 (2) | 55.5 | 0.172 | oblique45 | 0.063 | detection ضعيف |
| Video Project 2mk | **17.8** | 0.137 | oblique45 | 0.047 | detection بينهار — **مش جانبي** |
| Video Project 2 | 77.1 | 0.397 | other | 0.042 | مرفوض |
| Recording …171515 | 100 | 0.279 | other | 0.025 | مرفوض — مافيهوش تمرين |

**الخلاصة لسه هي هي، وأقوى: كليب واحد بس (`videoplayback (3)`) هو side view حقيقي**،
والإشارة القطنية معرّفة للـ side بس. يعني النطاق المرجعي **مايتبنيش أصلاً** — LOSO محتاج
شخصين على الأقل و`deadbug band` بيطلّع صفر bands ويقول ليه.

ملفات `Video Project 2*` التلاتة على الأغلب **تعديلات متداخلة لمصدر واحد** (747 / 2011 / 2833 فريم، نفس الدقة) — الـ detection الضعيف بتاعهم سببه شاشات عناوين وتمارين تانية، مش تصوير وحش. محتاجين `start_s`/`end_s` وده بالظبط غرض `clips.csv`.

### الإشارة على أنضف كليب

```
det = 100%  ·  frames = 899  ·  size = 640×352
floor: y = 0.0000x + 139.0   inliers = 1.000   (percentile, n=1557)
lumbar_gap: min=0.01299  med=0.01651  max=0.01896
view_score(px) = 0.113 → side
```

المدى ضيق (0.013–0.019)، وده **متوقع لكليب صحيح** — الظهر مفروض يفضل نازل. بس ده بالظبط اللي بوابة ١ موجودة تختبره: من غير reps مقوّسة مش هنعرف هل الفصل حقيقي ولا الإشارة مسيطر عليها offset هندسي ثابت.

---

## الجزء ٩ — العائق الوحيد المتبقي

> **بوابة ١ مش قادرة تشتغل.**
>
> تعريفها: "٥ reps صح مقابل ٥ reps مقوّسة، منفصلين بالعين."
> **مفيش ولا rep مقوّس على الديسك.**

مفيش أي بديل تقني لده. صلاحية الأداة (V1/V3) متثبتة، بس **صلاحية الأداة ≠ تقييم الكاشف**، ومفيش كمية من الداتا السليمة بتقفل الفجوة دي.

**المطلوب: ربع ساعة تصوير.**

```
موبايل على الأرض، جنبك، مترين
10 rep والظهر بيتقوّس عمداً
10 rep سريعة بدون وقفة
10 rep والحوض بيلف
10 rep صح            ← للتحقق من الاتساق
```

**وكمان** (مهم): مجموعة قصيرة عند **٠° و ٣٠° و ٤٥°** — عشان نعاير `view_score` على زوايا معروفة بدل ما نثق في الجدول الهندسي النظري. أجسام الناس بتختلف، وعتبة 0.12 ممكن تكون صارمة أكتر من اللازم.

**الـ label بييجي من النية وقت التصوير** — مصدر مستقل تماماً عن الإشارة. مشروع منهجياً ١٠٠٪.

---

## الجزء ١٠ — إزاي تشغّل المشروع

### البيئات

```bash
# البيئة الأساسية (المسار B): mediapipe + opencv
python -m venv venv && ./venv/Scripts/python.exe -m pip install -r requirements.txt

# بيئة المسار A منفصلة عمداً — aeon بينزّل numpy/pandas/scipy
uv venv venv-a --python 3.13
uv pip install --python venv-a "aeon==1.5.0" tensorflow keras matplotlib pyarrow pyyaml
```

⚠️ **ثبّتي `aeon==1.5.0` صراحةً.** قيد زي `numpy<2.5` بيخلي الـ resolver يقع على `aeon==0.0.0` الفاضي و**ينجح** بـ exit code 0.

### الاختبارات

```bash
./venv/Scripts/python.exe -m pytest tests/ -v
```

`pythonpath = ["src"]` في `pyproject.toml` بتخلي كل حاجة تشتغل من جذر الريبو من غير أي تثبيت.

### المسار A

```bash
./venv-a/Scripts/python.exe scripts/run_masar_a.py --sanity   # فولد واحد، ٥٠ epoch
./venv-a/Scripts/python.exe scripts/run_masar_a.py            # كامل
```

الـ sanity run غرضه الوحيد إنه يخلص من غير exception. **متبصّيش على رقمه.**

---

## الجزء ١١ — حدود المشروع

> **This system detects deviations from a reference movement pattern. It is not a
> diagnostic or clinical tool, was not validated on a patient population, and must
> not be used to guide medical decisions.**

- **مش أداة طبية.** بيكتشف انحراف عن نمط مرجعي، خلاص.
- **عدد الأشخاص صغير.** أي معدل إنذار كاذب لازم يتقال مع **فترة ثقة Wilson** وبعدد الأشخاص في الجملة نفسها — مش في هامش. "0/48 rep، 95% CI [0، 7.4%]، LOSO على N شخص" أقوى بكتير من "0% إنذار كاذب".
- **الحساسية شغل جارٍ.** لسه مفيش تقييم على أخطاء حقيقية.
- **مفيش dataset عام** لتمارين مستلقية بـ form labels — اتأكدنا.

**الصراحة دي بترفع المشروع مش بتنزّله.** ادعاء فايدة إكلينيكية بيخلي الشغل يبان ساذج قدام أي حد يعرف المجال.
