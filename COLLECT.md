# للفريق — تجميع الحركات

> ابعتي اللينك ده للشخص اللي هيصوّر. مش محتاج يبعتله أي ملف تاني.
>
> **https://github.com/asmaasamyhagag22-design/Dead-Bug**

---

## اللي هو محتاج يعمله

### ١. يجهّز (مرة واحدة، ٥–١٠ دقايق)

```bash
git clone https://github.com/asmaasamyhagag22-design/Dead-Bug.git
cd Dead-Bug/deadbug-aqa
powershell -ExecutionPolicy Bypass -File setup.ps1
```

`setup.ps1` بينزّل موديل الـ pose (٣٠ م.ب)، ويبني البيئة **على القرص المحلي**، ويشغّل
الاختبارات. آخر سطر لازم يقول `All tests passed`.

> **مايحتاجش** يستلم `data/clips/` ولا `data/rehabpile/` — دول مالهمش لزمة للتصوير.

### ٢. يقرا [FILMING.md](FILMING.md)

فيها البروتوكول بالتفصيل. الملخص في الجدول تحت.

### ٣. يصوّر، ويتأكد من نفسه قبل ما يبعت

```powershell
$py = "$env:USERPROFILE\venvs\deadbug\Scripts\python.exe"
& $py scripts\run_app.py          # localhost:8000 -> تبويب Upload
```

يرفع كل فيديو صوّره ويشوف: **العدد صح؟** و**لوحة triage خضرا؟**
لو التطبيق قال `oblique45` أو حذّر من حركة الكاميرا — **يعيد التصوير على طول**، مش بعد
ما يبعت.

### ٤. يبعت الفيديوهات الخام

بس. الفيديوهات نفسها، من غير قص ولا تعديل ولا export تاني.

---

## بروتوكول التصوير — الملخص

| | |
|---|---|
| الكاميرا | موبايل **على الأرض**، العدسة في مستوى الحوض |
| المسافة | **٢ متر** جنب الشخص، عمودية عليه (**side view**) |
| الوضع | **أفقي** · **720p كفاية** |
| الأرض | باينة على الحافة السفلية، مفيهاش زحمة |
| اللبس | **ضيق** — التيشرت الواسع بيخلي الـ silhouette يتبع القماش مش الضهر |
| الإضاءة | متساوية، **مفيش شباك ورا الشخص** |
| قبل كل take | نوم ثابت **٣ ثواني** · الركب ٩٠° |

### أربع فيديوهات، ١٠ عدات لكل واحد

| # | الحالة | يعمل إيه | الملف |
|---|---|---|---|
| 1 | `arched` | **يسيب ضهره السفلي يتقوّس** في كل عدة | `<اسمه>_arched.mp4` |
| 2 | `fast` | فورمة صح، **من غير أي وقفة** | `<اسمه>_fast.mp4` |
| 3 | `rotated` | **يسيب حوضه يلف** ناحية الرجل اللي بتتمد | `<اسمه>_rotated.mp4` |
| 4 | `correct` | صح تماماً — **يصوّره آخر واحد** | `<اسمه>_correct.mp4` |

**\+ ٣ مقاطع ٢٠ ثانية فورمة صح** عند **٠° / ٣٠° / ٤٥°**:
`<اسمه>_angle_00.mp4` · `_angle_30.mp4` · `_angle_45.mp4`

> **حطي اسمه في كل ملف.** ده بيوفر خطوة تعيين `person_id` بإيدك بعدين، وتعيين غلط منها
> بيحط نفس الشخص على جنبي الـ LOSO split ويخلي كل رقم بعدها متفائل من غير ما حاجة تكشفه.

---

## قاعدتين ممنوع تتكسروا

**١. بالغوا في الأخطاء.** عدة مقوّسة بالعافية والنظام مايشوفهاش **ماتقولش حاجة** — مش
هنعرف الأداة فشلت ولا الغلطة ماكانتش موجودة. خلوا `arched` واضح جداً.

**٢. الـ label بييجي من النية وقت التصوير، ومايتغيرش بعدين.** لو الرقم طلع مش عاجبكم،
**سيبوه**. المصدر المستقل ده هو اللي بيخلي التقييم مش دائري — والكود نفسه بيرفض أي
manifest فيه `label_source` غير `intent`.

---

## ليه ده مهم — لو حد سأل

كل الفيديوهات اللي عندنا دلوقتي **صح**. يعني نقدر نثبت إن النظام **بيقيس** ارتفاع الضهر،
ومانقدرش نثبت إنه **بيكتشف** إن حد قوّس ضهره. دول ادعاءين مختلفين.

**والفيديوهات دي هي الفرق بين الاتنين.**

وكمان: كل شخص جديد بيحسّن النطاق المرجعي أكتر من عشر فيديوهات زيادة من نفس الشخص، لأن
النطاق بيتبني **leave-one-subject-out** — لما نشيل الشخص اللي بنحكم عليه لازم يفضل غيره.

---

## لو حاجة وقعت عنده

| المشكلة | الحل |
|---|---|
| `setup.ps1` مش راضي يشتغل | `powershell -ExecutionPolicy Bypass -File setup.ps1` |
| `pose model not found` | التحميل فشل — اللينك في `HANDOVER.md §1` |
| `ModuleNotFoundError` | يشغّل `setup.ps1` تاني |
| التطبيق بيقول **٠ عدات** | calibration = **3** و baseline = **3**، مش أعلى |
| الكاميرا مرفوضة في المتصفح | لازم `localhost` أو HTTPS → يستخدم تبويب **Upload** |
